---
description: Capture a feature-scoped snapshot of the published Project Context.
---

# Cohesive Feature — Capture Context

For feature run `$ARGUMENTS`, read `.aidlc/skills/cohesive-feature-workflow.md`, then execute only `Phase: capture-context`. Write `docs/epics/$ARGUMENTS/artifacts/PROJECT-CONTEXT-SNAPSHOT.md`, summarize context risks, then tell the user to Mark Step Done.

