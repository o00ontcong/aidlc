---
description: Prepare an isolated branch and worktree for one work package.
---

# Cohesive Work Package — Prepare Worktree

For worker run `$ARGUMENTS`, read `.aidlc/skills/cohesive-work-package-workflow.md`, then execute only `Phase: prepare-worktree`. Resolve targets before mutation, never delete an unexpected worktree, write `WORKTREE-STATE.json`, then tell the user to Mark Step Done.

