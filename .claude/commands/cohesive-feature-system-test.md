---
description: Run configured project-level quality, integration, regression, and build checks.
---

# Cohesive Feature — System Test

For feature run `$ARGUMENTS`, read `.aidlc/skills/cohesive-feature-workflow.md`, then execute only `Phase: system-test`. Run the exact configured commands, write `SYSTEM-TEST-REPORT.md` with commands and exit codes, summarize GO/NO-GO, then tell the user to Mark Step Done.

