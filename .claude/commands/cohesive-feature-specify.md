---
description: Specify one cohesive feature against the current Project Context.
---

# Cohesive Feature — Specify

For feature run `$ARGUMENTS`, read `.aidlc/skills/cohesive-feature-workflow.md`, then execute only `Phase: specify`. Stop if title/description/inputs do not contain meaningful intent. Write `SPEC.md`, summarize requirements and open questions, then tell the user to Mark Step Done.

