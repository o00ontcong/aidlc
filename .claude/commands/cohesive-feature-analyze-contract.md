---
description: Analyze cross-artifact consistency and freeze the common Feature Contract.
---

# Cohesive Feature — Analyze and Contract

For feature run `$ARGUMENTS`, read `.aidlc/skills/cohesive-feature-workflow.md`, then execute only `Phase: analyze-contract`. Write `ANALYSIS.md`; only on GO write/freeze `FEATURE-CONTRACT.md` and synchronize its real hash into `WORK-PACKAGES.json`. Summarize the verdict, then tell the user to Mark Step Done.

