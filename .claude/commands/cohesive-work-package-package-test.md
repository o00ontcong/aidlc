---
description: Run task and package-level tests inside the package worktree.
---

# Cohesive Work Package — Test

For worker run `$ARGUMENTS`, read `.aidlc/skills/cohesive-work-package-workflow.md`, then execute only `Phase: package-test`. Run exact required commands inside the worktree and write `PACKAGE-TEST-REPORT.md` with GO or NO-GO. Then tell the user to Mark Step Done.

