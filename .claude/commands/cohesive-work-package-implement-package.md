---
description: Implement all related tasks in one approved package inside its isolated worktree.
---

# Cohesive Work Package — Implement

For worker run `$ARGUMENTS`, read `.aidlc/skills/cohesive-work-package-workflow.md`, then execute only `Phase: implement-package`. Work only inside the recorded worktree and write scope. Maintain `IMPLEMENT-STATE.md`, write `PACKAGE-SUMMARY.md`, and create a Change Request instead of silently changing frozen contracts. Then tell the user to Mark Step Done.

