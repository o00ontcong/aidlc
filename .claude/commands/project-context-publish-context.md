---
description: Publish the reviewed Project Context manifest with revision, source commit, and artifact hashes.
---

# Project Context — Publish Context

For run `$ARGUMENTS`, read `.aidlc/skills/project-context-workflow.md`, then execute only `Phase: publish-context`. Compute real SHA-256 hashes, write `CONTEXT-MANIFEST.json`, summarize the published revision, then tell the user to Mark Step Done.

