---
description: Decompose the feature and group related tasks into cohesive parallel-safe work packages.
---

# Cohesive Feature — Tasks and Packages

For feature run `$ARGUMENTS`, read `.aidlc/skills/cohesive-feature-workflow.md`, then execute only `Phase: tasks-package`. Write `TASKS.md` and `WORK-PACKAGES.json`. Prefer a few cohesive packages, validate dependencies/write scopes, summarize ready and blocked packages, then tell the user to Mark Step Done.

