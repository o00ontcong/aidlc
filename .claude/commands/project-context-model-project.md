---
description: Model project architecture, domain, shared contracts, and engineering rules from repository evidence.
---

# Project Context — Model Project

For run `$ARGUMENTS`, read `.aidlc/skills/project-context-workflow.md`, then execute only `Phase: model-project`. Produce every required context document under `docs/project/context/`, summarize material uncertainties, then tell the user to Mark Step Done.

