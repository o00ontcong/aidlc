# Retired — Cohesive Feature Coordinator

This agent is **not** attached to any live Cohesive Delivery 3.0 pipeline.

Use `feature-spike-agent` and `feature-implement-agent`. If a leftover workspace still lists `cohesive-feature-agent`, run **AIDLC: Migrate**.
