---
name: map-system
description: Map SkyCast layers and state ownership from concrete source evidence.
argument-hint: "<FOUNDATION-EPIC>"
---

# Map system — $ARGUMENTS

Write `docs/project/foundation/ARCHITECTURE-MAP.md`. It must include `## Layer Map`, name `Presentation → Data → Domain`, and cite real Swift paths. Make `ForecastStore` the state owner and `WeatherProviding` the replaceable boundary. Do not invent a network integration: this fixture is intentionally offline and deterministic.
