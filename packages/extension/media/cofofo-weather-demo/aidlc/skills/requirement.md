---
name: requirement
description: Convert a weather-product brief into testable acceptance criteria.
argument-hint: "<EPIC-KEY>"
---

# Requirement — $ARGUMENTS

Read the current `CONTEXT-MANIFEST.json`, `PROJECT-RULES.json`, and the epic inputs. Write `REQUIREMENT.md` with scope, non-goals, `## Acceptance Criteria`, and explicit user-facing wording. For the seeded heat-alert feature, require a threshold comparison in `ForecastStore`, no decision logic in SwiftUI, and deterministic unit tests. Do not start production mutation here.
