---
name: select-ecc-catalog
description: Select only vetted text contracts used by this offline fixture.
argument-hint: "<FOUNDATION-EPIC>"
---

# Select catalog — $ARGUMENTS

Write `ECC-CATALOG-SELECTION.md` with `## Approved Text Assets`. List the local `.aidlc/skills/*.md` assets needed for Swift, TDD, review, verification, memory and improvement; state that validators/hooks are AIDLC-owned and that the demo fetches nothing at runtime. Reject executable external assets and include the selected path plus SHA-256 for each text asset. This is a Canvas decision, not an agent-only choice.
