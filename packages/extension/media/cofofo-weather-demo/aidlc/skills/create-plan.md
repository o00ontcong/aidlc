---
name: create-plan
description: Plan a small SkyCast vertical slice with a concrete RED/GREEN contract.
argument-hint: "<EPIC-KEY>"
---

# Create plan — $ARGUMENTS

Read `REQUIREMENT.md`, `PROJECT-RULES.json`, and `ARCHITECTURE-MAP.md`. Write `TASK-PLAN.md` with `## RED / GREEN Contract`, a file-level task table, rule IDs, acceptance-criterion-to-test mapping, and a no-scope-creep boundary. For the heat alert, name the failing test exactly `testHighTemperatureAlertRequiresThreshold`; explain why it will fail before changing `ForecastStore`. This plan must receive a Canvas verdict before RED.
