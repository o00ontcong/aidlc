# Pipeline definitions are generated by generatedCofofoWorkspace() when this
# demo is seeded. Keeping this asset metadata-only makes recipe assembly part
# of the fixture instead of hiding it behind hand-written copies of pipelines.
version: "1.0"
name: "SkyCast CoFoFo Weather Demo"
description: >-
  Workspace SwiftPM đơn stack để kiểm thử CoFoFo end-to-end. Trình seed tạo
  Foundation/delivery pipelines từ generatedCofofoWorkspace và materialize
  từng run từ recipe thật.
environment: {}
