# SkyCast — project instructions

Các lệnh và policy của CoFoFo được publish vào marker managed bên dưới bởi
`publish-context`. Không sửa phần marker trực tiếp; sửa `PROJECT-RULES.json` rồi chạy lại bước publish.

<!-- aidlc:cofofo-context start -->
Placeholder khi đóng gói. Demo loader sẽ thay block này bằng Foundation
revision và content hash đã validate trước khi mở workspace.
<!-- aidlc:cofofo-context end -->
