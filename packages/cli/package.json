{
  "name": "aidlc",
  "version": "3.0.0",
  "description": "AIDLC terminal CLI — AI-driven SDLC pipeline runner. Plan → Prototype → Design ∥ Test → Implement → Release with discovery gates, prototype phase, requirements traceability, and Spec Kit support.",
  "license": "MIT",
  "funding": "https://github.com/sponsors/hueanmy",
  "repository": {
    "type": "git",
    "url": "https://github.com/aidlc-io/aidlc"
  },
  "homepage": "https://github.com/aidlc-io/aidlc#readme",
  "bugs": {
    "url": "https://github.com/aidlc-io/aidlc/issues"
  },
  "keywords": [
    "aidlc",
    "claude",
    "ai",
    "workflow",
    "sdlc",
    "agents",
    "cli"
  ],
  "engines": {
    "node": ">=18"
  },
  "main": "./dist/bundle.js",
  "bin": {
    "aidlc": "./dist/bundle.js"
  },
  "files": [
    "dist/bundle.js",
    "dist/templates",
    "dist/tools",
    "dist/vendor",
    "dist/assets",
    "README.md",
    "AUTOMATION.md"
  ],
  "scripts": {
    "compile": "tsc -p ./",
    "build": "tsc -p ./",
    "clean": "rm -rf dist",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=dist/bundle.js --external:fsevents && rm -rf dist/templates && cp -R ../core/templates dist/templates && rm -rf dist/tools dist/vendor dist/assets && cp -R ../../tools dist/tools && mkdir -p dist/vendor && cp -R ../../vendor/annotron dist/vendor/annotron && cp -R ../extension/assets dist/assets",
    "test:redesign": "pnpm --filter @aidlc/core build && pnpm bundle && node test/redesign-cli-smoke.cjs",
    "test:cohesive-recovery": "pnpm --filter @aidlc/core build && pnpm bundle && node test/cohesive-recovery-smoke.cjs",
    "test:clean-install": "pack_dir=$(mktemp -d) && pnpm pack --pack-destination $pack_dir >/dev/null && node test/clean-room-install-smoke.cjs $pack_dir/*.tgz",
    "prepublishOnly": "pnpm bundle"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "devDependencies": {
    "@aidlc/core": "workspace:*",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "chalk": "^4.1.2",
    "chokidar": "^3.6.0",
    "cli-table3": "^0.6.5",
    "commander": "^11.1.0",
    "esbuild": "^0.21.0",
    "js-yaml": "^4.1.1"
  }
}
